export type Evidence={time:string;step:string;result:string};
export default function TeacherLens({skill,status,miscon,prereq,attempts,evidence}:{skill:string;status:string;miscon:string;prereq:string;attempts:number;evidence:Evidence[]}){return <aside className="teacher"><div className="live"><span></span> TEACHER LENS · LIVE</div><h3>{status}</h3><dl><dt>Current skill</dt><dd>{skill}</dd><dt>Misconception signal</dt><dd>{miscon}</dd><dt>Prerequisite to probe</dt><dd>{prereq}</dd><dt>Unsuccessful attempts</dt><dd>{attempts}</dd></dl><h4>Process evidence</h4><div className="evidence">{evidence.length===0?<p>Waiting for student work…</p>:evidence.slice(-4).reverse().map((e,i)=><div className="evidenceItem" key={i}><small>{e.time}</small><strong>{e.step}</strong><span>{e.result}</span></div>)}</div><p className="note">The teacher view records the path, not only the final answer.</p></aside>}
